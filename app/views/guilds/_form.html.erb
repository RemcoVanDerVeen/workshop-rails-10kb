<%= form_with(model: guild) do |form| %>
  <% if guild.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(guild.errors.count, "error") %> prohibited this guild from being saved:</h2>

      <ul>
        <% guild.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :motto, style: "display: block" %>
    <%= form.text_field :motto %>
  </div>

  <div>
    <%= form.label :founded_on, style: "display: block" %>
    <%= form.date_field :founded_on %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
